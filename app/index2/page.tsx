import Index2 from './Index2';

// Standalone preview build — intentionally not linked from anywhere on the
// site yet. Visit directly at /index2/ to review the look before it's wired
// into any real page.
export default function Index2Page() {
  return <Index2 />;
}
