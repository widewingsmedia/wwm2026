import Hero from './Hero';

// Standalone, independent page — no site header/footer/chat widget
// (see the `/heropage` check in app/layout.tsx). Design ported verbatim
// from the Claude Design canvas export "Wide-Wings-Hero".
export default function HeroPage() {
  return <Hero />;
}
